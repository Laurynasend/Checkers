package LEcheckers;

import java.util.ArrayList;
import java.util.Collection;
import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;

import ai.MinMax;
import ai.ZobristValues;
import chess.board.Board;
import chess.board.EmptyTile;
import chess.board.Move;
import chess.board.Tile;
import chess.gui.Table;
import chess.pieces.Man;
import chess.pieces.Piece;
import chess.pieces.Side;

public class LEcheckers {
	
	public static void main(String[] args) {
		
	Table table=new Table();
	
		
	/*Scanner in=new Scanner(System.in);
	int startingCoordinate;
	int destinationCoordinate;
	Board.startingBoard();
	Board.drawBoard();
	
	MinMax testas=new MinMax();

	while(true) {
		if(Board.getPlayerToMove()==Side.WHITE) {
		startingCoordinate=in.nextInt();
		destinationCoordinate=in.nextInt();}
		else {
		Move bestMove=testas.ComputerMove();
		startingCoordinate=bestMove.getStartingCoordinate();
		destinationCoordinate=bestMove.destinationCoordinate;}
		Board.updateBoard(startingCoordinate, destinationCoordinate);
		testas.updateBoard(Board.getPlayingBoard(), Board.getPlayerToMove());
		Board.drawBoard();
	}*/
	}
	
}
